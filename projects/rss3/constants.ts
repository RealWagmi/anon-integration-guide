import { ChainId } from '@heyanon/sdk';

export const supportedChains = [ChainId.ETHEREUM, ChainId.OPTIMISM, ChainId.BSC, ChainId.GNOSIS, ChainId.POLYGON, ChainId.BASE, ChainId.AVALANCHE, ChainId.ARBITRUM];

export const rss3AIEndpoint = 'https://ai.rss3.io/api/v1';
